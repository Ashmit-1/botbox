/**
 * Backend Configuration
 * 
 * Single source of truth for backend URL.
 * All API calls should use this constant.
 */

export const BACKEND_URL = "http://127.0.0.1:8000";

export default {
  BACKEND_URL,
};
