from dataclasses import dataclass


@dataclass
class RequestContext:
    """
    Placeholder for future per-request context.
    Future fields: request_id, auth_context, feature_flags, etc.
    """
    pass